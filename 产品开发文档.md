# 光彩云村庄 · 产品开发文档

| 项 | 内容 |
|---|---|
| 项目名 | 光彩云村庄（云乡） |
| 工程根目录 | `D:\Abin\abincheung\WEB\云乡` |
| 文档版本 | v0.2（2026-08-31：新增 SaaS 多租户 + 统一双登录） |
| 日期 | 2026-08-31 |
| 技术栈 | Laravel 12 + Blade + Alpine.js + Tailwind SSR（零构建） |
| 关联文档 | `D:\Abin\doc\`：商业机会分析 / 产品需求文档PRD / MVP技术方案 / 数据库及后期拓展设计 |
| 状态 | 开发总纲，随开发迭代 |

> 本文件是工程根目录的开发总纲；策划/PRD/技术方案/DB 设计在上表 4 份关联文档中，本文件引用并落地为可执行规格。

---

## 1. 项目概述

**一句话**：把父亲分给我的 6 亩宁夏红寺堡光彩村枸杞地，切成可认养单元，以「云乡民」会员制租给城里人——以 6 亩为品牌样板田，逐步做成「云村庄」平台。

**产品形态（2026-08-31 升级）**：**通用 SaaS 多租户系统**。光彩云村庄不仅是自家 6 亩的认养平台，而是可复用的「云村庄」SaaS：接全村/国内/国外合作农户时，每个合作方开通一个**租户空间**（独立前台 H5 + 商户后台 + 家人端，数据隔离），平台统一运营、收订阅费/抽佣。MVP 范围不变（自家 1 租户先行验证），但架构与数据模型按 SaaS 底座设计，后期**只加租户行、不改主结构**。

**MVP 目标**：跑通 6 亩枸杞 **认养 → 监控 → 溯源 → 采收 → 配送** 闭环，验证付费意愿、信任建立、续费率。

**关键认知**：¥5000/分地价位下，枸杞只覆盖约六成年费，**剩四成是体验产品**——直播/内容/监控是产品本体核心交付，非营销。

**决策记录（已锁定）**：见 PRD §17；2026-08-29/30 新增 —— 运营主体=宁夏花乌巷食品（交易/收款/资质/有机认证）+ 西安青狐互动（内容/直播）；丰欠共担/保底细则、farm_members 定名、检测报告表、促销四件套（DB 设计 §9）；**2026-08-31：SaaS 化、微信端双登录**。

---

## 2. 技术栈与工程环境

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | Laravel 12 + PHP 8.2 | 2026-08-31 开工确认：安装器默认 12，与 11 同栈同构（bootstrap/app.php 同代），Signify 模式可复用 |
| 前端 | Blade + **Vite 构建** + **Tailwind v4**（`@vite` 注入，`@theme` 语义色，1455 行 CSS 设计系统） | 纯 Blade + 内联 JS，无 React/Vue；设计系统含完整 token 分层 + 组件库 |
| 数据库 | SQLite（dev）→ MySQL 或续 SQLite（prod） | 与 Signify 一致 |
| 认证 | Laravel Auth（session）+ laravel-wechat | **双登录：微信一键 + 账号密码**（§5） |
| 多租户 | 轻量自研：tenants 表 + tenant_id 全局 Scope + 租户中间件 | 或 stancl/tenancy；先自研（§4） |
| 支付/登录 | laravel-wechat + yansongda/laravel-pay | 微信 JSAPI；商户号主体=花乌巷食品 |
| 后台 | **手写 Blade** + Tailwind v4 设计系统（含 sidebar 抽屉 / 折叠 / 移动端 off-canvas） | 未采用 Filament，全部自研组件 |
| 短信 | 验证码服务（找回密码/手机绑定，V1） | 预留 |
| 部署 | 宝塔/Docker 自托管，挂 FOXFUN.CN 子域 | 如 `yun.foxfun.cn` |
| 对象存储 | 阿里 OSS / 腾讯 COS（图文/视频） | 本地起步可 |
| 直播 | 太阳能 4G 摄像头 + 萤石/阿里云 SDK | 不自建 |
| Composer | `php C:/Users/admin/composer.phar` | 本机不在 PATH |

**网络约束**（同 Signify）：git push/克隆常被阻断；若托管 GitHub 用 **Git-Data API** 落地（PAT 不打印）。

**预期目录结构**
```
云乡/
├─ app/Models/            # Tenant, Farm, Plan, Plot, Adoption, User, FarmMember, ...
├─ app/Tenancy/           # TenantScope, TenantMiddleware, TenantContext
├─ app/Http/Controllers/  # 按端分：Platform/Admin/Family/Site
├─ routes/                # platform.php(平台) + web.php(租户前台) + admin.php + family.php
├─ database/migrations/
├─ resources/views/       # platform/ site/ admin/ family/ components/
├─ config/                # wechat, pay, storage, camera, tenancy
├─ 产品开发文档.md         # 本文件
└─ README.md
```

---

## 3. 系统架构（四层，SaaS）

```
平台层（SaaS）——平台后台 /platform/*
  租户开通/套餐/计费/主题模板/聚合看板
  │
  ├─ C 端 H5（云乡民，微信内）  /t/{slug}
  ├─ 商户后台（每租户运营）     /t/{slug}/admin
  └─ 家人移动端（每租户录入）   /t/{slug}/family

Laravel SSR 单一后端，tenant_id 行级隔离，web guard 统一认证（双登录）
SQLite/MySQL + 对象存储 + 第三方直播云
```

---

## 4. SaaS 多租户架构

### 4.1 租户模型
- `tenants`（租户）= 一个独立「云村庄」运营空间。**MVP 首个租户 = 光彩云村庄**（运营主体花乌巷食品）。
- 关系：`tenants 1—N farms 1—N plots`；认养方案 `plans`、结算 `settlements` 均挂租户。
- 平台方 = 云村庄 SaaS（管理全部租户），平台自身公司作为平台级主体。

### 4.2 隔离策略
- **单库共享表 + `tenant_id` 行级隔离**（推荐）：一人维护、成本最低，几十~几百租户足够。
- 实现：全局 `TenantScope`（自动过滤）+ `TenantMiddleware`（从路径 `/t/{slug}` 解析当前租户注入上下文）；无租户上下文的写操作拦截。
- 不采用库级/Schema 级隔离（成本高、用不上）。
- 备用方案：`stancl/tenancy` 包（需要完整领域事件/资源切换时再上），自研先跑通。

### 4.3 路由形态
- **路径式 `/t/{slug}` 起步**：微信网页授权回调域名只需一个，最省事。
- 后期可加子域 `<slug>.yun.foxfun.cn`（中间件兼容两种解析）。
- 路由三组：平台 `/platform/*`（无租户）、租户前台 `/t/{slug}/*`、公共 `/`（选店入口）。

### 4.4 套餐与计费（V2 商业化，MVP 建表占位）
- `plans`（平台套餐，区别于认养方案）：免费/基础/专业——租户数、田块数、摄像头数、抽佣费率、主题可配。
- 平台收入：租户订阅费 + 认养流水抽佣（settlements/payouts 按租户分账）。
- MVP 自家 1 租户不启用计费。

### 4.5 租户个性化
- `tenants.settings`：主题 token 可配（各租户自己的品牌色/名）。
- 因此 UI 规范 §9 的 token 需做**两层**：平台默认 token + 租户覆盖 token。

### 4.6 多租户支付与分账（关键待定）
- **MVP（自家 1 租户）**：花乌巷食品商户号直接收款，无分账。
- **V2 接合作农户**（选一）：
  - A) **平台统一收单 + 分账**：平台商户号收款 → 微信「分账」或 T+N 提现后按 settlements 结算给租户（推荐）
  - B) 各租户自带商户号：需各租户 appid/商户号对应与资质，开通成本高
- 列入 §15 待定，V2 前定。

---

## 5. 统一认证（双登录）

### 5.1 users 表（账号统一，两路登录归一）
| 字段 | 说明 |
|---|---|
| phone | 可空唯一；**账号密码登录主键** |
| username | 可空，替代登录名 |
| password | 可空（纯微信用户无密码，bcrypt） |
| openid / unionid | **微信一键登录**凭证 |
| tenant_id | 归属租户 |
| role | 云乡民 / 家人 / 租户管理员 / 平台管理员 |
| password_set_at | 设密时间（审计） |
| nickname, avatar, real_name, village_card_no, joined_year | 资料与会员字段 |

### 5.2 登录流程
1. **微信一键登录**：`/t/{slug}/login/wechat` → 网页授权 openid → 命中：写 session 登录；未命中：自动建号（默认云乡民）+ 引导完善昵称/绑定手机
2. **账号密码登录**：`/t/{slug}/login` 表单（phone/username + password）→ Laravel session auth
3. **身份合一**：web guard 共用；登录后按 `role + tenant_id` 决定进入前台 / 商户后台 / 家人端
4. **绑定升级**：微信用户可「绑定手机号 + 设密码」→ 双登录可用；手机号已存在 → 提示合并
5. **找回密码**：手机验证码重置（V1；MVP 可先客服人工）

### 5.3 安全
bcrypt、登录限流（throttle）、CSRF、绑定/合并/退款等敏感操作二次确认、密码与 openid 关联审计、防账号碰撞（微信+手机双身份不暴露他端凭据）。

---

## 6. 数据模型

以 `D:\Abin\doc\光彩云村庄-数据库及后期拓展设计.md` 为准。SaaS 化后新增/变更：
- **新增 `tenants` 表**（slug、settings、套餐、状态）
- **所有业务表统一 `tenant_id`**（plots/adoptions/users/farm_logs/...），全局 Scope 过滤
- **`users` 加 auth 字段**（phone/username/password/openid/unionid/password_set_at，见 §5.1）
- `plans` 分两层：平台套餐（V2）/ 认养方案（现有，挂租户）

**三根桩仍成立**：主体（orgs/farms）→ 标的（多态 adoptable）→ 方案（plans），如今都挂租户；后期拓展只加行/加表。

**丰欠共担/保底（DB 设计 §4.1）**：一分地保底 15kg、基准 20kg、超产 50% 加赠封顶 25kg；欠收补至保底（库存补果或 ¥150/kg 折算退费）；严重欠收 <10kg 差额退费 + 下季优先；不可抗力退费或顺延。株档走拼团田池均摊，保底 0.5kg/株。

---

## 7. 功能与页面清单

以 MVP 技术方案 §4 为准，SaaS 化后：

- **公共**：`/` 选店入口（平台首页）
- **租户前台 H5（C 端）**：`/t/{slug}` 首页、`/adopt` `/adopt/{plot}` `/adopt/{plot}/order` `/my/plot/{adoption}`(`+/name`) `/live` `/trace/{plot}` `/s/{code}` `/harvest` `/gift` `/shop` `/me`
- **登录**：`/t/{slug}/login`（账号密码）、`/t/{slug}/login/wechat`（微信一键）、找回密码
- **商户后台**：`/t/{slug}/admin` 田块/订单/内容/监控/溯源/物流/用户/财务/数据/家人子账号；**含缺产补/退流程**（adoption_adjustments，走查补充）
- **家人移动端**：`/t/{slug}/family` 发动态 / 录有机肥批次 / 录采收 / 开直播预告（按 farm_members.permission_scope 限权）
- **平台后台**：`/platform/*` 租户管理（开通/停用/套餐/主题）、聚合看板、对账

---

## 8. 外部接口

| 接口 | 用途 | 主体/备注 |
|---|---|---|
| 微信登录 | 网页授权 openid（一键登录） | 认证服务号 + 网页授权域名 |
| 微信支付 JSAPI | 认养/礼盒/增值品 | 商户号=花乌巷食品（待核实）；多租户分账见 §4.6 |
| 短信验证码 | 找回密码/手机绑定（V1） | 预留 |
| 快递物流 | 运单/轨迹 | 干果耐储，标快即可 |
| 摄像头云 | 直播/延时/回看 | 萤石/阿里云 SDK |
| 对象存储 | 图文/视频 | OSS/COS |
| 检测机构 | 检测报告 | V1 起，报告入 detection_reports |

---

## 9. UI 与前端规范（**采用 Airbnb 设计规范**）

> 两处落地：**UI 结构方法 = Airbnb DLS（Design Language System）分层**；**前端代码规范 = Airbnb JavaScript Style Guide（eslint-config-airbnb 子集，适配 Alpine.js）**。

### 9.1 设计方向（品牌）
真实感为骨（真实照片 > 插画渐变）+ 在地文化点缀（印章/铭牌）+ 现代清晰保转化。信任是第一卖点。token 分**平台默认 + 租户覆盖**两层（§4.5）。

### 9.2 设计 token（Tailwind config / CSS 变量落地）
| Token | 值 | 用途 |
|---|---|---|
| 枸杞红（主色） | `#B33A26` | CTA / 品牌 / 直播角标 |
| 赭石金（辅助） | `#C9A227` | 丰收 / 礼盒 / 超产 |
| 有机绿（点缀） | `#5F7A54` | 溯源 / 有机标识（克制使用） |
| 米白 / 燕麦（底） | `#FAF6F0` / `#F1EAE0` | 页面底色 |
| 深墨（字） | `#2B2620` | 正文 |
| 标题字体 | 思源宋体 Noto Serif SC | 在地感；微信内加载不稳 → 子集化或系统 serif fallback |
| 正文字体 | 系统无衬线 | 可读性 |
| 圆角 | 8–12px；礼盒/铭牌偏方 | 宋体配直角更在地 |
| 印章 | SVG 内联（乡民卡/铭牌/「已核验」） | 不引字体库 |

### 9.3 按 Airbnb DLS 分层组织 UI（Tokens → 基础 → 业务 → 页面）
| 层 | 内容 | 本工程实现 |
|---|---|---|
| **Tokens** | 色彩/字体/间距/圆角 | §9.2 → Tailwind config + `:root` CSS 变量（平台默认 + 租户覆盖） |
| **基础组件** | 按钮/表单/卡片/标签/弹窗 | Blade 组件 + Alpine（daisyUI/WeUI 起手，覆盖品牌 token） |
| **业务组件** | 直播窗 / 田块地图(SVG) / 铭牌生成 / 溯源时间线 / 签名板(Canvas) / 礼盒卡 | 独立 Blade 组件（Alpine 交互） |
| **页面** | 平台 / C 端 H5 / 商户后台 / 家人端 四套页面 | 按路由组织 |

### 9.4 前端代码规范（Airbnb JavaScript Style Guide 子集，适配 Alpine）
- 缩进 2 空格、单引号、**无分号**（Airbnb 核心约定）
- 优先 `const`，避免 `var`；用箭头函数与命名函数
- 组件/事件名 kebab-case；不污染全局（Alpine `x-data` 封装）
- 注释：必要处说明"为什么"而非"是什么"
- 页面数据尽量服务端渲染，Alpine 只做交互增强（零构建约束）
- PHP 侧并行：PSR-12 + Laravel 官方约定（瘦控制器、FormRequest/Service 分层）

### 9.5 四端 UI 规范
| 端 | 风格 | 组件来源 |
|---|---|---|
| 平台后台 | 冷静高效、租户列表、聚合看板 | Filament 或 daisyUI |
| C 端 H5 | 真实影像 + 印章点缀；转化组件高对比 | WeUI 规范 + daisyUI（品牌 token 覆盖） |
| 商户后台 | 表格密集、数据看板优先 | **Filament**（省一半后台工作量）或 daisyUI 手搓 |
| 家人移动端 | 大字号、大按钮、拍照为主 | daisyUI 简化，不做设计感 |

---

## 10. 构建顺序（3 Sprint）

以 MVP 技术方案 §8 为准，SaaS 化后 Sprint 1 追加租户骨架与双登录：

- **Sprint 1｜地基、租户与认养（2–3 周）**：migrations + Models（§6 全表含 tenants）→ **租户骨架**（TenantScope + 中间件 + /t/{slug}）→ **双登录**（微信一键 + 账号密码 + 绑定）→ 后台账号 + C 端微信登录 → plots 切分录入（50 分地 + 10 拼团田 + 株）→ 田块列表/详情 + 下单（支付 mock/人工）→ 田块 CRUD、名额状态机
- **Sprint 2｜闭环主干（2–3 周）**：微信支付真接（**先核实花乌巷食品商户号/食品许可**）→ 我的田（命名/日历/铭牌）+ 监控嵌入 → 家人移动录入端 → 溯源时间线 + NXLB 批次 + 溯源码扫码
- **Sprint 3｜交付与礼盒（2 周）**：采收/配送 + **补/退流程** → 节日礼盒 + 亲笔签 → 续费/增值券 → 数据看板；硬件并行（摄像头选型安装）

**MVP 成功标准**：签约种子用户 ≥ 目标数、闭环无断点、季末续费意向率 ≥ 目标、信任相关差评可控（数值待定，见 PRD §14）。

---

## 11. 测试与质量

- 后端：Pest/PHPUnit 单元 + 功能测试（认养状态机、支付回调、名额并发、补退计算、溯源码唯一、**认证双登录/绑定/限流**、**租户隔离**——越权访问他租户资源必须 403/404）
- 前端：无单测框架（零构建），靠**手工验收清单**（以「用户操作流程走查」为底稿：认养→支付→命名→直播→溯源→采收→配送→礼盒→续费）
- QA 重点：支付回调幂等、缺产补/退正确性、直播断流降级、家人端弱网可用、售罄拦截、权限（family scope）、租户数据隔离
- 上线前走查：支付主体核实、食品资质、有机话术合规（未取证只写「生态/有机肥种植」）

---

## 12. 部署与运维

- 宝塔/Docker 自托管，FOXFUN 子域；dev SQLite / prod MySQL（或续 SQLite）
- **租户开通流程**：建租户 → 配路径/子域 → 初始化基础数据（farms/plots/plans 模板）
- 代码托管 GitHub 走 **Git-Data API**（PAT 不打印；git push 常被阻断）
- 密钥管理：微信 appId/商户号、对象存储、摄像头云 key 全部走环境变量，不入库不入仓
- 备份：数据库定时备份（含租户粒度恢复）；对象存储异地冗余
- 日志：Laravel 日志 + 支付回调日志；异常告警（断流/掉线/支付回调失败）

---

## 13. 合规要点（引用 PRD §13）

| 项 | 要求 |
|---|---|
| 食品经营许可 | 花乌巷食品须覆盖「枸杞/干果/预包装食品销售」（待核实范围） |
| 有机 | 转换期 2–3 年；未取证前标「有机转换产品」或只写「生态/有机肥种植」 |
| 资金监管 | 预付认养费专户 / 分阶段释放；丰欠共担补退在采收结算统一执行；多租户分账见 §4.6 |
| 隐私 | 监控画面涉家人/他人肖像，注意授权与边界；**多租户数据隔离**，租户间数据不可串 |
| 认证合规 | 手机号/openid 最小化收集；敏感操作二次确认 |
| 出口（V3） | FDA 注册/USDA Organic/产地证，出口前 6–12 月启动 |

---

## 14. 里程碑与 KPI

MVP（0–3 月）→ V1（3–9 月，闭环+内容+有机认证启动+续费复盘）→ V2（9–18 月，**多租户启用接全村**/家禽/农旅/社区/B端）→ V3（18 月+，国内→国外合作农户）。KPIs 见 PRD §15（认养数/续费率/产出达标率/溯源查看率/监控日活等）；**SaaS 附加指标**：开通租户数、每租户认养数、平台抽佣/GMV。

---

## 15. 待核实 / 待定

1. **花乌巷食品**：食品经营许可范围、是否已开微信商户号、出资结构/法人（**Sprint 2 支付真接前必须**）
2. 6 亩是否已在有机转换期；直播谁出镜
3. 丰欠共担示范值随自家亩产实测校准（保底 15kg 等）
4. 6 亩土地权属/流转手续（公司作认证主体需要）
5. 认养资金监管落地（专户/分阶段释放）
6. 对象存储与摄像头云平台选型（OSS/COS、萤石/阿里云）
7. **多租户支付分账方案**（§4.6：平台统一收单+分账 vs 各租户自带商户号）——V2 接合作农户前必须定
8. SaaS 套餐定价 / 抽佣费率（V2 定）
