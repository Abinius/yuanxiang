# 光彩云村庄 · 开发计划表单

| 项 | 内容 |
|---|---|
| 版本 | v0.1 |
| 日期 | 2026-08-31 |
| 依据 | `产品开发文档.md` §10（3 Sprint + SaaS 骨架 + 双登录）、DB 设计、MVP 技术方案 |
| 开发方式 | Abin 一人主开发（复用 Signify 栈），家人只做录入与现场 |
| 总工时 | 约 **40 人日**（S1 15 + S2 15 + S3 10），5 天/周专注约 8 周，目标 6–8 周完成 MVP |
| 状态图例 | ⬜ 未开始 ｜ 🔄 进行中 ｜ ✅ 完成 ｜ ⏸ 阻塞 ｜ ✅＊ 代码就绪（待外部数据/前置，填好即上线不改码） |

> 当前进度（2026-08-31）：Sprint 1 基本 ✅（除 1.6 后台账号体系）；**Sprint 2 全部 ✅（2.1–2.8）、Sprint 3 全部 ✅（3.1–3.6）、分享与站点设置模块 ✅、143 测试全绿，上线走查见 `上线走查.md`**（2.1 待 P1 商户号 / 2.3 待 P3 摄像头 / 2.5 待 P4 批次 / 2.7 待 P6 服务号即真接上线）。剩 M2 真支付联调与 1.6 后台账号体系。

---

## 1. 里程碑

| 里程碑 | 目标 | 验收标准 | 周次 |
|---|---|---|---|
| **M1** | 地基 + 租户 + 认养 | 微信一键 + 账号密码登录可用；浏览→下单（mock 支付）→命名闭环；租户隔离生效 | W2–W3 末 |
| **M2** | 闭环主干 | 微信支付真接；家人端可录入；溯源时间线 + 扫码可查 | W5–W6 末 |
| **M3** | 交付上线 | 配送/补退/礼盒/续费全闭环；种子用户可上线认养 | W8 末 |

> 每周排期速览见 §7；里程碑日期按开工日起排。

---

## 2. 前置条件（商务/现场，不占开发人日，**立即启动**）

| # | 前置项 | 负责人 | 阻塞什么 | 截止 |
|---|---|---|---|---|
| P1 | **花乌巷食品：食品经营许可范围 + 微信商户号** | Abin | Sprint 2 支付真接（硬前置） | W4 前 |
| P2 | 6 亩地块测绘切分 + 编号 + 地块牌（现场） | 父亲/弟弟 | plots 录入与 C 端地图 | W2 前 |
| P3 | 摄像头选型 + 采购 + 安装（太阳能 4G ×2–4） | 弟弟 | Sprint 2 监控嵌入 | W4 前 |
| P4 | NXLB 有机肥批次信息/检测资料 | 弟弟 | 溯源 + 检测报告 | W4 前 |
| P5 | 直播露脸人选 + 账号素材（故事/照片） | Abin/家人 | 内容与首屏 | W3 前 |
| P6 | 服务号认证 + 网页授权域名配置 | Abin | 微信一键登录 | W1 末 |

---

## 3. Sprint 1｜地基、租户与认养（约 15 人日）

| # | 任务 | 交付物 / 验收 | 依赖 | 人日 | 状态 |
|---|---|---|---|---|---|
| 1.1 | 项目 scaffold：Laravel 12 + 目录结构 + .env/SQLite + 基础配置（评估复用 Signify） | `composer create-project` 完成；`php artisan serve` 可跑；APP_NAME=光彩云村庄 | — | 1 | ✅（2026-08-31；安装器默认 12，与 11 同栈同构，升级确认） |
| 1.2 | Migrations 全表（含 tenants + tenant_id + users 认证字段；plans/plots seed 归 1.7） | `migrate:fresh` 零报错，34 张表全建 | 1.1 | 2 | ✅（11 个迁移全跑通） |
| 1.3 | Models + 关系（多态 adoptable、Scopes、软删） | 枚举 cast/租户隔离/多态关系/软删验证通过 | 1.2 | 2 | ✅（25 模型 + 9 枚举 + TenantScope 验证） |
| 1.4 | 租户骨架：TenantContext + TenantScope + TenantMiddleware + `/t/{slug}` 解析 | 无租户上下文写操作被拦截；/t/guangcai 200、不存在 404 | 1.2 | 1.5 | ✅（中间件按 slug 解析，防闭包无类型提示时绑定失效） |
| 1.5 | 双登录：微信一键（自研 OAuth，dev mock）+ 账号密码 + 绑定合并 | 两路登录/错误密码 422/绑定接口通过 | 1.4 | 2.5 | ✅（账号密码+mock微信全流程 7 项通过；真实微信待 P6 配 appid；找回密码待 V1 短信） |
| 1.6 | 平台后台 + 商户后台账号体系（角色/权限基础） | 后台登录可进 | 1.5 | 1 | ⬜ |
| 1.7 | 认养方案 seed：plans（分地 5000/株 300）+ plots 切分（50 分地 + 10 拼团田 + 300 株） | 田块数据分布正确；PlotSeedTest 覆盖 | 1.2 | 1.5 | ✅（dev 库 360 单元就位） |
| 1.8 | C 端田块列表/详情 + 下单（**支付 mock/人工确认**）+ 地址 | 下单闭环可跑 | 1.7 | 2.5 | ✅（列表/详情/拼团田/下单/mock 支付/地址闭环；AdoptFlowTest 通过，含跨租户隔离与拼团田分支；修隐式路由绑定缺失 Tenant 类型提示、enum 与字符串严格比较两处前置 bug） |
| 1.9 | 名额状态机（待支付→待签约→已生效）+ 售罄拦截 + 命名 | 售罄正确拦截、命名写入 | 1.8 | 1 | ✅（signAgreement→生效+地块置已认养；拼团田末株认养→整田售罄；非法状态签约 422；命名铭牌写入；4 项通过） |

**M1 验收**：双登录可用；浏览→下单（mock）→命名全流程；切换 `/t/other` 数据隔离。

---

## 4. Sprint 2｜闭环主干（约 15 人日）

| # | 任务 | 交付物 / 验收 | 依赖 | 人日 | 状态 |
|---|---|---|---|---|---|
| 2.1 | 微信支付真接（yansongda/laravel-pay JSAPI + 回调幂等 + 退款） | 支付→回调→订单生效；重复回调无副作用 | **P1** | 3 | ✅＊ |

> ＊2.1 代码就绪：JSAPI 下单 / 回调验签落单（按全局 `adoption_no` 幂等）/ 后台退款三路径已实现，48 测试全绿（含重复回调无副作用、重复退款幂等）。**真接待 P1**：花乌巷食品商户号凭证到位后，填 `.env`（`WECHAT_MCH_ID`/`WECHAT_APIV3_KEY`/商户证书）即可切换 `WECHAT_PAY_MODE=normal` 上线，无需改码。W4 前凭证未到则按 §8 用 mock 支付跑闭环、Sprint 3 前必须真接。
| 2.2 | 我的田：生长日历 + 农事动态流 + 铭牌生成/分享 | 田块主页完整 | 1.8 | 2.5 | ✅＊ |

> ＊2.2 完成：`MyPlotController` 三路由（`/my` 列表 / `/my/plot/{adoption}` 主页 / `/my/plot/{adoption}/nameplate` 分享页），owner-gated + 状态守卫（非主人 404、非生效 403）。铭牌抽 `site/partials/nameplate` 复用于 success + 主页 + 分享页（分享页带"已核验"印章 SVG + 复制链接）。生长日历 12 月横条按 `config/goji.php` 红寺堡物候着色 + 当月事件计数 + 今天标记。农事动态流取 `is_public` 且 `plot_ids` 隔离（分地/拼团/单株三类型覆盖），按 `occurred_at` 倒序、空态有文案。`FarmLogSeeder` 撑 dev 数据。57 测试全绿（新增 9 项）。合规：全链路无"有机"字样（cert 未启动），"有机肥基施"仅作投入描述。
| 2.3 | 监控嵌入：摄像头管理 + `/live` 播放器（厂家 SDK/HLS）+ 断流降级 | 直播/延时/回看可用 | **P3** | 2 | ✅＊ |

> ＊2.3 代码就绪：摄像头后台管理六路由（tenant_admin）+ 云乡民 `/live` 列表与单路播放。播放器 hls.js（CDN），Safari 走原生 HLS；两级断流降级——服务端按 `status`+`stream_url` 分支、客户端 hls.js `ERROR` 事件再兜底，离线/无流渲染降级面板（4G/供电中断文案 + 可选 `playback_url` 回看）。`CameraSeeder` mock：FD-01 online（公开 HLS 测试流）、FD-02 offline 演示降级。71 测试全绿（新增 LiveTest 6 项）。**真实流待 P3**：硬件到位后在后台填 `stream_url`/`token`、置 `status=online` 即上线，不改码。合规/隐私：`/live` auth 守卫（不公开），页面标注"画面涉及家人肖像，已获授权"；无"有机"字样。
| 2.4 | 家人移动端：登录 + 发动态/录肥/录采收/直播预告 + scope 权限 | 家人端四类录入可用 | 1.5 | 2.5 | ✅＊ |

> ＊2.4 完成：家人端四类录入可用——`/family` 按 `farm_members.permission_scope` 限权（基类 `assertScope` 统一守卫：tenant_admin 直通、无 membership 403、scope 缺失 403）。三录入面：发农事动态/直播预告（`FarmLogController` 统一表单，type 下拉含 `LiveBroadcast`，图片入本地盘 `farm-logs/`，`is_public` 默认公开喂"我的田"动态流）、有机肥批次（`FertilizerBatchController`，NXLB 投入品信息，非有机认证声明）、采收（`HarvestController`，`handler_id`=录入人）。`storage:link` 已建。71 测试全绿（新增 FamilyEntryTest 8 项）。图片 OSS/COS 待 V1 按 env 切磁盘，不改模型。
| 2.5 | 溯源：farm_logs 节点 + NXLB 批次（**P4**）+ 溯源时间线页 | `/trace/{plot}` 时间线完整 | 2.4 | 2 | ✅＊ |

> ＊2.5 完成：公开溯源时间线 `/trace/{plot}`（无 auth，信任卖点配合认养前转化；2.6 扫码页复用同一 `trace-timeline` partial）。时间线合并 farm_logs(is_trace_node) + harvests 表 + detection_reports，统一节点按时间升序；施肥节点带 NXLB 批次卡（batch_no/成分/检测链接，差异化主线）；采收以 harvests 表为权威、同日 farm_log 叙事并入去重。家人端联动：施肥/采收/检测自动 `is_trace_node=true`，施肥记录可选批次挂 `fertilizer_batch_id`（补上 2.4 的批次↔日志断链）。认养详情页加"查看溯源"入口。78 测试全绿（新增 TraceTest 5 + FamilyEntryTest 2）。**P4 真数据未到**：mock TraceSeeder 联调，真资料到位由家人端录入或换 Seeder 即上线不改码。合规：仅"有机肥（NXLB）投入品"文案，无认证宣称。
| 2.6 | 溯源码：生成/打印 + `/s/{code}` 扫码页 | 每箱一码可扫 | 2.5 | 1.5 | ✅＊ |

> ＊2.6 完成：每箱一码可扫——`TraceCodeService::generate` 按采收批量生成唯一箱码（`TC{Ymd}-{8随机大写}`，DB 唯一索引+查重双保险）；后台 `/admin/trace-codes` 生成→跳打印页（CDN qrcode.js 前端出 QR，`@media print` 标签纸，CDN 不可用降级显示 code 明文）。公开扫码页 `/t/{slug}/s/{code}`：租户前缀 + TenantScoped 自动隔离（跨租户码 404），每次扫码 `scanned_count`+1（喂溯源查看率 KPI），页内本箱采收卡 + 认养 CTA + 成为云乡民入口（拉新预留），复用 `trace-timeline` partial。节点组装抽为 `TraceService`（TraceController 与扫码页共用，2.5 的 TraceTest 5 项保持绿）。86 测试全绿（新增 ScanTest 8）。**adoption_id 本期留空**，与 deliveries/gift_boxes 的箱级关联留 Sprint 3；QR 绝对 URL 依赖请求 host，上线配域名即可。合规：扫码页/标签仅"有机肥（NXLB）投入品"，无认证宣称。
| 2.7 | 内容/直播预告推送（模板消息） | 云乡民收得到开播提醒 | 2.3 | 0.5 | ✅＊ |

> ＊2.7 完成：直播预告/内容动态推送——家人发 live_broadcast/daily 公开动态 → `SendFarmLogNoticeJob`（队列）推本租户全部 active 认养人（去重、openid 非空）。`WechatTemplateService` mock 模式只落 `push_messages`（status=sent）不真发；P6 服务号认证后填 `WECHAT_TEMPLATE_LIVE_NOTICE/CONTENT` + appid/secret 即真发（`cgi-bin/message/template/send`），不改码。96 测试全绿（新增 PushTest 5）。合规：推送文案禁"有机产品/有机认证"。
| 2.8 | 后台补齐：订单/内容/监控/溯源管理 | 后台可运营 | 2.1–2.6 | 1 | ✅＊ |

> ＊2.8 完成：后台可运营——订单列表（`/admin/adoptions`，状态/支付中文标签 + 仅已支付可退款）、农事内容列表（`/admin/farm-logs`，软删）、后台首页导航修复（统计卡可点击 + 快捷管理：订单/内容/摄像头/溯源码）、`AdoptionStatus`/`PaymentStatus` 补 `label()`、退款补 mock 分支（`config('wechat.mock')` 下模拟成功，无凭证 dev 可测）。96 测试全绿（新增 AdminOpsTest 5）。

**M2 验收**：真支付闭环；家人端可在田里录数据；溯源时间线 + 扫码出全程。

> 2026-08-31 进度：家人端录入 ✅、溯源时间线 ✅、扫码可查 ✅；**真支付闭环待 P1 商户号凭证**。

---

## 5. Sprint 3｜交付与礼盒（约 10 人日）

| # | 任务 | 交付物 / 验收 | 依赖 | 人日 | 状态 |
|---|---|---|---|---|---|
| 3.1 | 采收/配送：harvest 录入 + 规格选择 + 打单 + 运单/签收 | 采收通知→收货确认全链路 | 2.8 | 2.5 | ✅＊ |

> ＊3.1 完成：采收配送全链路——家人录采收 → `SendHarvestNoticeJob` 推该田块认养人「你的田今天采了」；后台 `/admin/deliveries` 按采收为 active 认养人打单（取默认/最新地址写 `deliveries.address_id`，跳过重复）→ 录运单发货（tracking/carrier/shipped_at）→ 打单打印（@media print）；认养人在「我的田」看配送进度 + 确认收货（received_at）。`DeliveryStatus` 补 `label()`。108 测试全绿（新增 DeliveryTest 8）。合规：文案仅"保底分装"，无认证宣称。
| 3.2 | **缺产补/退流程**（adoption_adjustments）+ 保底规则引擎（delivery_rule 落地） | 欠收按 §4.1 规则补/退正确 | 3.1 | 2 | ✅＊ |

> ＊3.2 完成：保底规则引擎（DB §4.1）——后台按年度结算生成 `adoption_adjustments`（欠收按 ¥150/kg 折算退费、严重欠收附「下季优先认养权」、平年/丰收不记录、幂等去重），apply 走部分退款（`requestRefund` 加金额参数，mock 分支）。`adoption_adjustments` 补 `season_year` 列。128 测试全绿（新增 AdjustmentTest 6）。
| 3.3 | 节日礼盒：定制 + 亲笔签（Canvas 签名板）+ 印制/发货 + 拉新 | 三节礼盒下单→收礼人转化 | 3.1 | 2 | ✅＊ |

> ＊3.3 完成：节日礼盒——权益额度（festival_quota 三节各 1，单株不开放）创建 + Canvas 亲笔签落盘 + 收礼人/寄语定制；后台制作/发货/送达 + 贺卡打印；公开扫码落地页（祝福/亲笔签 + 「成为云乡民」微信一键登录拉新）。`gift_boxes` 补 `code`/`carrier`/`shipped_at`/`received_at`，收礼人放宽可空。128 测试全绿（新增 GiftBoxTest 7）。
| 3.4 | 续费/促销券：coupons + 续费优惠 + 老带新推荐码 | 续费/推荐码可用 | 3.2 | 1.5 | ✅＊ |

> ＊3.4 完成：续费（新单下季，`renewed_from_id` 链，原单保留）+ 续费意向开关 + renewal 券抵扣（`coupon_usages` 记录，券后价写 annual_fee/Payment）+ 老带新推荐码（复用 coupons.code，下单填码互发新客/推荐人券）+ 促销后台（活动/券列表 + 新建）。`createOrder` 参数化（season_year/renewed_from_id/coupon，含券属主校验）。128 测试全绿（新增 RenewalPromotionTest 7）。推荐码「支付成功才发券」防薅留 V1。
| 3.5 | 数据看板：认养转化/续费意向/产出达标/溯源查看率 | 看板出核心指标 | 2.8 | 1 | ✅＊ |

> ＊3.5 完成：后台首页「经营看板」——认养转化率（生效/全单）、续费意向（auto_renew 生效单）、产出达标率（Σ采收 / Σ各单 `delivery_rule.guarantee_kg`）、溯源查看率（被扫箱数/总箱数）+ 年度简表。`DashboardController` 汇总统计，`dashboard.blade.php` 新增 KPI 区与配送卡。108 测试全绿（新增 DashboardStatsTest 4）。注：ended 恒 0（无到期路径）、auto_renew 数据待 3.4 续费才有。
| 3.6 | 端到端验收 + 上线走查（支付主体/资质/合规话术/种子用户） | 可上线售 | 全 | 1 | ✅＊ |

> ＊3.6 完成：端到端验收走查 + 上线清单已产出 `上线走查.md`——13 条主干流程逐条核对（各对应 Feature 测试全绿，128 项/420 断言）+ 上线清单七节（支付资金 P1 硬前置 / 服务号 P6 / 合规话术 / 部署运维 / 现场前置 P2–P5 / 种子用户 / 已知留待项）。结论：MVP 全闭环可跑（mock 支付），真实上线须先过 P1 商户号 + P6 服务号。

**M3 验收**：MVP 全闭环可运营；种子用户完成首认养。

> 2026-08-31 进度：3.1–3.6 已 ✅，上线走查见 `上线走查.md`；待 P1 商户号 + P6 服务号到位即真上线（M2 口径下 mock 支付已可跑通全闭环）。

---

## 6. 并行项（非开发）

| 项 | 说明 | 负责人 |
|---|---|---|
| 内容素材 | 首屏故事/照片/直播片段 | Abin + 家人 |
| 种子用户清单 | 亲友 + 内容粉丝首批名单 | Abin |
| 认证启动 | 有机认证申请资料（转换期长，早启动） | Abin + 花乌巷食品 |
| 土地权属/流转 | 6 亩承包/流转手续（公司作认证主体） | Abin + 父亲 |

---

## 7. 每周排期速览（MVP 8 周）

| 周 | 开发主线 | 并行/前置 |
|---|---|---|
| W1 | 1.1 scaffold ✅ → 1.2 migrations ✅ → 1.3 Models ✅ | P6 服务号；P2 测绘启动 |
| W2 | 1.4 租户 ✅ → 1.5 双登录 ✅ → 1.6 后台账号 ⬜ | P2 完成；P1 核实启动 |
| W3 | 1.7 plans/plots seed ✅ → 1.8 下单 ✅ → 1.9 名额 ✅/**M1** | P5 素材 |
| W4 | 2.1 支付真接（P1）✅＊ → 2.2 我的田 ✅ | P3 装摄像头；P4 肥批次 |
| W5 | 2.3 监控 ✅＊ → 2.4 家人端 ✅ | 家人培训（录入） |
| W6 | 2.5 溯源 ✅ → 2.6 溯源码 ✅ → 2.7 推送 ✅ → 2.8 后台补齐 ✅ → **M2** | — |
| W7 | 3.1 采收配送 → 3.2 补/退 | 采收季对齐 |
| W8 | 3.3 礼盒 → 3.4 促销 → 3.5 看板 → 3.6 上线/**M3** | 种子用户上线 |

---

## 8. 风险与缓冲

| 风险 | 影响 | 缓冲 |
|---|---|---|
| P1（商户号/许可）未按时 | Sprint 2 支付延期 | 先用 mock/人工支付继续跑闭环，Sprint 3 前必须真接 |
| 采收季 vs 开发进度 | M3 错过采收演示 | 采收是自然周期，Sprint 3 对齐；先用历史/样田数据联调 |
| 摄像头/网络不稳 | 直播断流 | 降级提示 + 延时短片为主 |
| 家人录入学习成本 | 数据缺失 | 极简端 + 一次线下培训（P2 时顺带） |

---

## 9. 任务卡片模板（每任务开工时填）

```
任务 #：________________    预估人日：____    依赖：#____
交付物/验收：________________________________
今日进度：
   ⬜ 完成项 / 🔄 进行项 / 阻塞说明
下步动作：
```

> 维护方式：本表单随开发在根目录 `开发计划.md` 更新状态；完成一项即打 ✅ 并留验收备注。

---

## 10. 分享与站点设置（额外模块，2026-08-31 ✅）

| 项 | 说明 | 状态 |
|---|---|---|
| 数据库设计 | `short_links` 表（code/target_url/click_count，唯一+软删+索引）+ `tenants.settings` json 键扩展（brand / seo / share / footer_copyright / icp_number / contact） | ✅ |
| 租户设置 | `Tenant::setting()` + `config/site.php` 平台默认（两层 token）；布局品牌色从 settings 动态输出（收敛两份硬编码）；修 title 重复拼接 | ✅ |
| SEO 分享 | `site/partials/seo`（title/description/keywords/OG/Twitter/canonical/robots）+ 页面级覆盖 + `/robots.txt` + `/sitemap.xml` | ✅ |
| 社交媒体分享 | `site/partials/share`（复制链接 / QQ / 微博 / 微信右上角转发提示）+ **公开铭牌落地页**（拆 auth+owner 锁死，外链可打开）+ 接入溯源/扫码/礼盒/推荐码 | ✅ |
| 短链接 | `ShortLinkService`（随机/自定义码唯一查重）+ `/u/{code}` 302 跳转 + 点击计数 + 后台生成/复制 | ✅ |
| 后台管理设置 | `/admin/settings`（品牌色/分享文案/SEO/备案/联系方式）+ `/admin/short-links` + dashboard 卡片/快捷导航 | ✅ |

> 完成备注：**143 测试全绿**（新增 SettingsTest 4 + ShortLinkTest 5 + SeoTest 4 + ShareTest 2）。微信 JSSDK 未做（P6 后可加 `WechatJssdkService`）；og:image 需绝对 URL；短链 302 跳转（微信预览走目标页 OG）。合规：分享/SEO 文案过「生态种植 / 有机肥（NXLB）投入品」口径，无认证宣称。
