# 光彩云村庄 · 端到端验收走查 + 上线清单（3.6）

> 日期：2026-08-31 ｜ 依据：`开发计划.md`（Sprint 1–3）、`产品开发文档.md`、DB 设计、PRD。
> **自动化验收基线：`php artisan test` → 128 项全绿（420 断言）。**
> 手动走查前建议：`php artisan migrate:fresh --seed` + `db:seed --class=AdminSeeder/FarmLogSeeder/CameraSeeder/TraceSeeder/TraceCodeSeeder/PromotionSeeder`。

## 一、端到端验收走查（全流程）

| # | 流程 | 入口 | 关键步骤 | 期望结果 | 自动化依据 |
|---|---|---|---|---|---|
| 1 | 双登录 | `/t/guangcai/login` | 账号密码登录 / mock 微信一键登录（openid 自动建号）/ 微信绑定手机 | 两路登录可用；微信用户绑定手机；换租户隔离 | `AuthTest` |
| 2 | 认养闭环 | `/adopt` → `/adopt/{plot}` | 浏览田块 → 下单（地址）→ mock 支付 → 签约命名 → 生效 | 订单 pending_payment→pending_agreement→active；地块置已认养；拼团田末株售罄 | `AdoptFlowTest` |
| 3 | 我的田 | `/my` → `/my/plot/{adoption}` | 铭牌 / 12 月生长日历 / 农事动态流 / 配送进度 / 礼盒入口 / 续费按钮 | 主人可见、非主人 404、非生效 403；动态流仅公开+按 plot 隔离 | `MyPlotTest`、`DeliveryTest` |
| 4 | 家人录入 | `/family`（父/弟/母按 scope） | 发动态（带图）/ 直播预告 / 录有机肥批次 / 录采收 | scope 限权（tenant_admin 直通）；施肥/采收/检测自动成溯源节点；采收触发通知 Job | `FamilyEntryTest`、`PushTest`、`DeliveryTest` |
| 5 | 溯源时间线 | `/trace/{plot}`（公开） | 浏览施肥(带 NXLB 批次卡)/检测/采收合并节点，按时间升序 | 公开无 auth；只含溯源节点；跨租户 404 | `TraceTest` |
| 6 | 溯源码扫码 | `/s/{code}`（公开） | 后台按采收生成箱码 → 打印 QR → 扫码 | 每箱一码唯一；扫码页 = 本箱采收+时间线+认养 CTA；`scanned_count`+1；跨租户 404 | `ScanTest` |
| 7 | 监控直播 | `/live`（auth） | 摄像头列表 → 单路 HLS 播放 / 离线降级 | 在线播放、离线降级面板；肖像授权 note | `LiveTest` |
| 8 | 配送闭环 | `/admin/deliveries` → 我的田 | 采收 → 打单 → 发货（运单）→ 认养人确认收货 | pending→shipped→delivered；打印打单；C 端配送进度 | `DeliveryTest` |
| 9 | 节日礼盒 | 我的田 → 礼盒 → 扫码 | 权益额度创建 → Canvas 亲笔签 → 后台制作/发货/送达 → 收礼人扫码落地 → 成为云乡民 | 额度校验（超额 422）；亲笔签落盘；公开落地页 | `GiftBoxTest` |
| 10 | 缺产补退 | `/admin/adjustments` | 按年度结算 → 欠收生成补退 → 应用（部分退款） | 欠收/严重欠收正确计算（¥150/kg）；幂等；平年不生成 | `AdjustmentTest` |
| 11 | 续费/推荐码 | 我的田 → 续费 / 我的推荐码 | 续费建下季单（可带 renewal 券）→ 分享推荐码 → 新客下单填码 | 新单 season_year+1、renewed_from_id；券抵扣写 annual_fee；互发券 | `RenewalPromotionTest` |
| 12 | 后台运营 | `/admin` | 订单/农事/摄像头/溯源码/配送/补退/礼盒/促销 + 经营看板 | 各管理页可运营；导航互通；tenant_admin 限定、villager 403 | `AdminAccessTest`、`AdminOpsTest`、`DashboardStatsTest` |
| 13 | 数据看板 | `/admin` 经营看板 | 认养转化率 / 续费意向 / 产出达标率 / 溯源查看率 / 年度简表 | 四类 KPI 可算可看 | `DashboardStatsTest` |

> 补充校验（贯穿全站）：**租户隔离**（跨租户一律 404，`TenantScoped`+显式守卫）；**合规**（cert_status=not_started → 全站无"有机产品/有机认证"，仅"生态/有机肥种植/有机肥投入品"）；**隐私**（监控肖像已授权 note、手机号/openid 最小化）。

## 二、上线走查清单

### A. 支付与资金 —— **上线硬前置（P1）**
- [ ] 花乌巷食品 **食品经营许可范围**核实（是否含枸杞干果零售）
- [ ] **微信商户号**（微信支付 v3）→ 填 `.env`：`WECHAT_MCH_ID`/`WECHAT_APIV3_KEY`/商户证书/`WECHAT_PAY_NOTIFY_URL`，置 `WECHAT_PAY_MODE=normal`（代码已就绪，2.1 ✅＊，无需改码）
- [ ] **认养费资金监管**：认养费进花乌巷对公账户专户/分账设计（待定，上线前落）
- [ ] 退款路径走查（后台退款 + 补退部分退款）用真实商户号核验

### B. 微信服务号（P6）
- [ ] 服务号认证 + **网页授权域名**配置 → `WECHAT_APP_ID`/`WECHAT_SECRET`（1.5 一键登录真接）
- [ ] 模板消息模板 ID → `WECHAT_TEMPLATE_LIVE_NOTICE/CONTENT/HARVEST_NOTICE`（2.7/3.1 真发）

### C. 合规话术（上线前走查核对）
- [ ] 全站文案无"有机认证/有机产品"（cert_status=not_started）；仅"生态/有机肥种植"、"有机肥（NXLB）投入品"
- [ ] 监控页面肖像授权说明在；家人/现场人员已获授权
- [ ] 检测报告：MVP 承诺占位（`detection_reports` 实测数据 V1 补）
- [ ] 推送/促销/礼盒文案无夸大、无违规宣称

### D. 部署与运维
- [ ] 域名 + HTTPS（微信支付/服务号回调、扫码绝对 URL 需要）
- [ ] 队列 worker（`QUEUE_CONNECTION=database` → supervisor 常驻，推送 Job 消费）
- [ ] `php artisan storage:link`（图片/签名对外可访问；OSS/COS 可按 env 切磁盘）
- [ ] `.env` 生产环境：APP_DEBUG=false、APP_ENV=production、SESSION/CACHE 配置
- [ ] 定时任务（若后续加季末结算/到期迁移，需 cron）

### E. 现场/数据前置（P2–P5）
- [ ] P2 地块牌（plots 已 seed 360 单元，现场牌确认）
- [ ] P3 摄像头安装 → 后台填 `stream_url`/`token` 置 online（2.3 上线）
- [ ] P4 NXLB 批次真实资料 → 家人端录入或替换 TraceSeeder（2.5 上线）
- [ ] P5 直播露脸人选 + 内容素材（故事/照片/直播片段）

### F. 种子用户
- [ ] 亲友 + 内容粉丝首批名单；上线前内测一轮（走 13 个流程）

### G. 已知留待项（V1 / 后续 Sprint，不阻塞 MVP 验收）
- [ ] 找回密码（V1 短信）
- [ ] 推荐码防薅（支付成功后才发 X/Y 券）
- [ ] 超产库存池/补果、部分退款 Payment 状态细化、不可抗力顺延兑现
- [ ] 1.6 商户后台账号体系 / 平台后台
- [ ] 礼品盒收礼人地址（当前走 giver 地址，收礼人自填留 V1）

## 三、结论

- **代码与自动化验收**：MVP 主干 13 条流程全部落地，**143 测试全绿（461 断言）**，各流程有对应 Feature 测试覆盖，可进入手工走查。
- **分享与站点设置**（2026-08-31 额外模块）：短链接 `/u/{code}`、租户设置 `/admin/settings`、SEO（OG/robots/sitemap）、公开铭牌落地页、分享组件已落地（见 `开发计划.md` §10）。
- **可上线售的硬门槛**：P1（支付主体/商户号）+ P6（服务号）到位前，可用 mock 支付跑通全闭环（M2 验收口径）；真实上线须先过 A/B 两节。
- 文档收尾：`开发计划.md` 3.6 与 §10 置 ✅＊。
